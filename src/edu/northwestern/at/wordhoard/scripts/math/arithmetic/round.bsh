import java.lang.Math;

import edu.northwestern.at.utils.math.ArithUtils;

double round( double x , int n )
{
	return ArithUtils.round( x , n );
}

long round( double a )
{
	return Math.round( a );
}

int round( float a )
{
	return Math.round( a );
}

